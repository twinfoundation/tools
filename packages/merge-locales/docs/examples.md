# @gtsc/merge-locales - Examples

## Command Line Tool

First install the tool with the following script.

```shell
npm install @gtsc/merge-locales
```

You can then run the tool from the command line e.g.

```shell
merge-locales
```
